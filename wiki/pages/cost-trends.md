# Cost trends

Auto-generated by `scripts/cost_trends.py` from `query_logs` (129 queries with logged cost).

## Average cost per query_type

| query_type | n | avg cost (USD) |
|---|---|---|
| comparison | 29 | 0.009187 |
| factual | 46 | 0.009455 |
| risk_analysis | 46 | 0.012678 |

**Observation:** Comparison queries cost ~1.0x factual ones ($0.009187 vs $0.009455 on average).

## Average cost per persona

| persona | n | avg cost (USD) |
|---|---|---|
| audit_firm | 9 | 0.011311 |
| investment_bank | 10 | 0.011255 |
| investment_firm | 63 | 0.011313 |
| legal | 16 | 0.009500 |
| treasury | 23 | 0.008933 |

## Average cost per status

| status | n | avg cost (USD) |
|---|---|---|
| abstained | 8 | 0.004104 |
| error | 20 | 0.000325 |
| valid | 101 | 0.012329 |

## Average cost with vs. without live ingestion

| query type | n | avg cost (USD) |
|---|---|---|
| with live ingestion | 5 | 0.050353 |
| without ingestion | 124 | 0.008329 |

**Observation:** A query that triggers live ingestion costs ~6x a regular query ($0.050353 vs $0.008329 on average) — ingestion makes one contextual-retrieval LLM call per chunk, hundreds per filing.

## What cannot be computed from query_logs

Section 7 of the spec also asks for a correlation on `retry_count`, but that isn't a persisted column in `query_logs` (schema.sql doesn't include it, and section 7 explicitly forbids adding new columns). It cannot be computed from existing data without an additional source (e.g. extracted from LangSmith traces, where retry_count exists in each node's state).
