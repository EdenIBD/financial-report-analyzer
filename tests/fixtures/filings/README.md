# Real SEC HTML excerpts

Metadata elements and Item/Part paragraphs with their next paragraph are extracted from the original documents. No section prose is fabricated. These are parser regression fixtures, not complete filings for ingestion. Full source URLs and SHA-256 hashes are in `eval/upload-fixture-manifest.json`.
